import "./styles.css";

// P0 scaffold stub. The real bootstrap (capture → tracking → gesture → menu
// router → execution → render) is wired in P3 (main.ts wiring). Keeping this
// minimal so `npm run build` validates the scaffold before features land.
console.log("DAEDALUS // P0 scaffold online");
